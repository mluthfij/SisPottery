<%= comment.content %>
<%= time_ago_in_words(comment.created_at) %>


<p>
   <%= link_to 'delete', [comment.product, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
</p>
<%= render partial: 'comments/form', locals: { product: comment.product, parent: comment } %>
<hr>

<div class="subcomment" style="padding-left: 20px;">
    <%= render comment.comments.order(id: :desc) %>
</div>
