<% unless params[:action] == 'new' %>
<div id="carouselExampleSlidesOnly" class="carousel slide" data-bs-ride="carousel">
  <div class="carousel-inner carousel-inner-custom bg-light bg-opacity-50">
    <%#  %>
    <%# <div class="carousel-item active"> %>
    
    <% if obj.attached? %>
      <% obj.each_with_index do |cover, index| %>
          <div class="carousel-item <%= index == 0 ? 'active' : '' %>" style="background-image: linear-gradient(rgba(0, 0, 0, 0.45), rgba(0, 0, 0, 0.45)), url('<%= cover.url %>');background-repeat: no-repeat;background-attachment: scroll;background-position: center center;background-size: cover;">
            <%# <%= image_tag cover.variant(loader: { page: nil }, coalesce: true, resize_to_limit: [500, 500]), class:"bg-light w-100" %>
          <%# <div class="carousel-caption d-none d-md-block">
              <h5>First slide label</h5>
              <p>Some representative placeholder content for the first slide.</p>
          </div> %>
          </div>
      <% end %>
    <% else %>
      <div class="carousel-item">
        <img src="https://img.freepik.com/free-photo/woman-showing-pinterest-icon_53876-40589.jpg?w=2000" class="card-img-top" alt="bg-light w-100 carousel-fit">
      </div>
    <% end %>

    <%# </div> %>
    
    <%#  %>
  </div>
</div>
<% end %>
