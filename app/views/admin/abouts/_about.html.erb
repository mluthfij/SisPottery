    <%# hidden when sm %>
    <div class="bg-light text-center p-5 mb-4 bg-light rounded-3 d-none d-sm-block">
      <div class="container-fluid">
        <h1 class="d-block fw-bold"><%= @about.title %></h1>
        <%= @about.sub_title %>
        <p class="col fs-4"><%= @about.description %></p>
        <button class="btn btn-primary btn-lg" type="button">Example button</button>
      </div>
    </div>

    <%# visible when sm %>
    <div class="bg-light text-center py-4 pb-4 mb-4 bg-light rounded-3 d-block d-sm-none">
      <div class="container-fluid">
        <h1 class="d-block fw-bold"><%= @about.title %></h1>
        <%= @about.sub_title %>
        <p class="col fs-4"><%= @about.description %></p>
        <button class="btn btn-primary btn-lg" type="button">Example button</button>
      </div>
    </div>

  <%# <%= @about.company_name %> 