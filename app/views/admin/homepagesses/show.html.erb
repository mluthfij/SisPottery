<div class="card mb-3 bg-light">
    <div class="card-body">
        <%= link_to 'Edit', edit_admin_homepagess_path(@homepagess), class:"btn btn-lg
        btn-outline-dark" %>
        <%= link_to 'Delete', admin_homepagess_path(@homepagess), class:"btn btn-lg
        btn-outline-dark", method: :delete, data: { confirm: "Are you sure?" } %>
        <%= link_to 'Exit', admin_pages_dashboard_path, class:"btn btn-lg btn-outline-dark
        float-end" %>
    </div>
</div>
<div class="border border-secondary">
    <%= render 'admin/homepagesses/home' %>
</div>
