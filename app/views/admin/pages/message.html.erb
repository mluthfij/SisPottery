<div class="container-fluid px-4">
    <h2 class="mt-4">
        Message page
    </h2>
    <hr>
    <% @allmsg.each do |msg| %>
        <div class="card <%= current_user.admin? ? 'bg-success' : 'light' %>">
            <p>
                <%= msg.inspect %>
            </p>
        </div>
    <% end %>

</div>