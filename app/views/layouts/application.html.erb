<!DOCTYPE html>
<html>
  <head>
    <title>SisPotteryGallery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column" style="padding-top:100px;">
      <%= render user_signed_in? && current_user.admin? ? 'layouts/adminnavbar' : 'layouts/navbar' %>
      <div id="page-content">
        <%= render 'layouts/alert' if flash.any? %>
        <% if user_signed_in? && current_user.admin? %>
          <h1 class="container">You are an admin</h1>
        <% end %>
        <%= yield %>
      </div>
    <%= render 'layouts/footer' %>
  </body>
</html>
